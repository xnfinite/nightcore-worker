[package]
name = "nc-exec"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Night Core — backend-agnostic execution interface"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["clock", "serde"] }
